{
  "name": "@mail-app/root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "dev": "lerna run dev --stream"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/**",
    "shared/**"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
